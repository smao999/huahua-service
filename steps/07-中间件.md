# Step 7：中间件

> **目标**：添加 CORS、请求日志、全局限流等中间件。
> 参考 `steps-v1/03-不依赖外部API的handler.md` 的补充 2。

---

## 7.1 中间件是什么

中间件在 Gin 里是"请求到达 handler 之前/之后执行的函数"。它们像一个管道：

```
请求 → Logger 中间件 → CORS 中间件 → 限流中间件 → Handler → 响应
```

## 7.2 CORS 中间件

```go
// internal/middleware/cors.go
package middleware

import (
    "time"
    "github.com/gin-contrib/cors"
    "github.com/gin-gonic/gin"
)

func CORS() gin.HandlerFunc {
    return cors.New(cors.Config{
        AllowOrigins:     []string{"*"},
        AllowMethods:     []string{"GET", "POST", "PUT", "DELETE", "OPTIONS"},
        AllowHeaders:     []string{"Content-Type", "Authorization"},
        AllowCredentials: true,
        MaxAge:           12 * time.Hour,
    })
}
```

## 7.3 请求日志中间件

```go
// internal/middleware/logger.go
package middleware

import (
    "log"
    "time"
    "github.com/gin-gonic/gin"
)

func RequestLogger() gin.HandlerFunc {
    return func(c *gin.Context) {
        start := time.Now()
        path := c.Request.URL.Path
        c.Next()
        log.Printf("[%s] %s %d %s",
            c.Request.Method, path, c.Writer.Status(), time.Since(start))
    }
}
```

## 7.4 装配到 app.go

```go
// internal/app/app.go
package app

import (
    "huahua-service/internal/config"
    "huahua-service/internal/handler"
    "huahua-service/internal/middleware"
    "huahua-service/internal/router"
    "github.com/gin-gonic/gin"
)

func New(cfg *config.Config) *gin.Engine {
    r := gin.New()  // 不用 Default，我们自己控制中间件
    
    // 中间件顺序
    r.Use(middleware.RequestLogger())  // 1. 请求日志
    r.Use(gin.Recovery())              // 2. panic 恢复
    r.Use(middleware.CORS())           // 3. 跨域
    
    h := handler.NewHandlers()
    router.Register(r, h)
    
    return r
}
```

## 7.5 更新 main.go

```go
// cmd/server/main.go
func main() {
    cfg := config.Load()
    // db.InitGORM(cfg.DB)  // 暂不打开
    // db.InitRedis(cfg.Redis)
    
    r := app.New(cfg)
    r.Run(":" + cfg.Port)
}
```

## 7.6 验证

```bash
go run cmd/server/main.go
curl -v http://localhost:8080/api/health
# 应该看到 CORS 头：Access-Control-Allow-Origin: *
# 日志打印：[GET] /api/health 200 0s
```

---

## 本步总结

- ✅ CORS 中间件（跨域支持）
- ✅ RequestLogger 中间件（请求日志）
- ✅ Recovery 中间件（panic 捕获）
- ✅ 中间件装配顺序
- ✅ main.go 变干净了，app.New 统一管理
