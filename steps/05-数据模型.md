# Step 5：数据模型

> **目标**：定义数据库表对应的 Go 结构体（GORM Model）。
> 参考 `steps-v1/01-基础设施先行.md` 的 1.3 节。

---

## 5.1 为什么现在才加模型

前 4 步已经让服务能跑、能读配置、能连数据库。
现在是时候告诉 GORM"数据库里有哪些表，每张表长什么样"了。

## 5.2 核心模型清单

Python 项目有 11 张表，Go 重构需要逐个映射为 GORM 结构体：

```go
// internal/model/user.go — 用户表
type User struct {
    ID             int64      `gorm:"primaryKey;autoIncrement"`
    Username       string     `gorm:"uniqueIndex;size:50"`
    Email          string     `gorm:"uniqueIndex;size:100"`
    HashedPassword string     `gorm:"size:255;column:hashed_password"`
    Avatar         string     `gorm:"size:255;default:''"`
    Nickname       string     `gorm:"size:50;default:''"`
    UID            string     `gorm:"uniqueIndex;size:32"`
    InvitedBy      *int64     `gorm:"column:invited_by"`
    VIPExpiresAt   time.Time  `gorm:"column:vip_expires_at"`
    ProExpiresAt   *time.Time `gorm:"column:pro_expires_at"`
    IsAdmin        bool       `gorm:"column:is_admin;default:false"`
    CreatedAt      time.Time  `gorm:"column:created_at;autoCreateTime"`
}

func (User) TableName() string { return "users" }
```

```go
// internal/model/fund_basic_info.go — 基金基本信息
type FundBasicInfo struct {
    Code         string    `gorm:"primaryKey;size:6"`
    Name         string    `gorm:"size:100"`
    Type         string    `gorm:"size:20"`
    ConfirmDays  int       `gorm:"column:confirm_days;default:1"`
    FeesJSON     string    `gorm:"column:fees_json;type:text"`
    HoldingsJSON string    `gorm:"column:holdings_json;type:text"`
    UpdatedAt    time.Time `gorm:"column:updated_at;autoUpdateTime"`
}

func (FundBasicInfo) TableName() string { return "fund_basic_info" }
```

```go
// internal/model/fund_nav.go — 基金历史净值
type FundNav struct {
    ID       int64   `gorm:"primaryKey;autoIncrement"`
    FundCode string  `gorm:"column:fund_code;index;size:6"`
    Date     string  `gorm:"index;size:10"`
    Nav      float64 `gorm:"type:numeric(10,4)"`
    Change   float64 `gorm:"type:numeric(7,4)"`
}

func (FundNav) TableName() string { return "fund_navs" }
```

**完整 11 张表的代码见**：`steps-v1/01-基础设施先行.md` 的 1.3 节。

---

## 5.3 自动迁移

在 main.go 中添加 AutoMigrate，让 GORM 自动创建/更新表结构：

```go
// 在 db.InitGORM 之后加上：
db.GORM.AutoMigrate(
    &model.User{},
    &model.FundBasicInfo{},
    &model.FundNav{},
    // ... 其他模型
)
log.Println("数据库迁移完成")
```

---

## 本步总结

- ✅ User 模型（用户表）
- ✅ FundBasicInfo 模型（基金信息表）
- ✅ FundNav 模型（历史净值表）
- ✅ GORM AutoMigrate 自动建表
