# Step 14：补充业务 + 静态文件 + 测试 + 联调

> **目标**：补齐剩下所有 handler（Admin/Version/Agent/JCTI/Public），
> 加静态文件托管、测试、端到端验证。
> 参考 `steps-v1/10-Agent相关.md`~`steps-v1/14-查缺补漏.md`。

---

## 14.1 剩余 handler 清单

| Handler | 路由前缀 | Python 文件 | 说明 |
|---|---|---|---|
| Admin | `/api/admin/*` | `routers/admin.py` | 用户管理、VIP 操作 |
| Version | `/api/version` | `routers/version.py` | 版本信息 |
| AgentRequest | `/api/agent/*` | `routers/agent_request.py` | AI Agent 请求 |
| JCTI | `/api/jcti/*` | `routers/jcti.py` | 人格评测 |
| Public | `/api/public/*` | `routers/public.py` | 博客统计 |

每个 handler 的三层结构完全一样：
```
router/xxx.go → handler/xxx.go → service/xxx/ → repository/xxx.go
```

## 14.2 注册全部路由

```go
// internal/router/router.go — 最终版
func Register(r *gin.Engine, h *handler.Handlers, mw *middleware.Middleware) {
    api := r.Group("/api")
    registerHealth(api, h.Health)
    registerAuth(api, h.Auth)
    registerFund(api, h.Fund)
    registerMarket(api, h.Market)
    registerUser(api, h.User, mw)
    registerAdmin(api, h.Admin, mw)
    registerVersion(api, h.Version)
    registerAgentRequest(api, h.AgentRequest, mw)
    registerJcti(api, h.Jcti, mw)
    registerPublic(api, h.Public)
}
```

## 14.3 静态文件 + SPA 回退

```go
// internal/app/app.go 中追加
r.Static("/static/uploads", "./data/uploads")
r.NoRoute(func(c *gin.Context) {
    if len(c.Request.URL.Path) >= 4 && c.Request.URL.Path[:4] == "/api" {
        c.JSON(404, gin.H{"detail": "API endpoint not found"})
        return
    }
    c.File("./dist/index.html")
})
```

## 14.4 测试

```go
// internal/handler/health_test.go
package handler

import (
    "net/http"
    "net/http/httptest"
    "testing"
    "github.com/gin-gonic/gin"
)

func TestHealthPing(t *testing.T) {
    gin.SetMode(gin.TestMode)
    r := gin.New()
    h := NewHealthHandler()
    r.GET("/api/health", h.Ping)

    req := httptest.NewRequest("GET", "/api/health", nil)
    w := httptest.NewRecorder()
    r.ServeHTTP(w, req)

    if w.Code != http.StatusOK {
        t.Errorf("期望 200，得到 %d", w.Code)
    }
}
```

## 14.5 最终验证脚本

```bash
# 1. 编译
go build ./...

# 2. 运行测试
go test -v ./...

# 3. 手动验证关键接口
curl http://localhost:8080/api/health
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"test","password":"Pass1234","email":"test@test.com"}'
```

## 14.6 完整检查清单

- [ ] Gin 服务启动成功
- [ ] /api/health 正常响应
- [ ] 配置从环境变量读取
- [ ] PostgreSQL + Redis 连接
- [ ] GORM 自动迁移
- [ ] 10 组路由全部注册
- [ ] CORS / 日志 / Recovery 中间件
- [ ] 缓存层 + JWT + bcrypt
- [ ] 注册/登录接口正常
- [ ] Auth 中间件验证通过
- [ ] HTTP 客户端 + 数据源
- [ ] Fund/Market 业务接口
- [ ] AI 调用（OpenRouter/Gemini）
- [ ] 夜估校准 + 后台任务
- [ ] 剩余 handler 全部注册
- [ ] 静态文件 + SPA
- [ ] 测试通过

---

## 全部步骤回顾

从 Step 1 的 30 行代码到 Step 14 的完整项目，每一步都可以独立验证：

| Step | 验证方式 | 外部依赖 |
|---|---|---|
| 1 启动 Gin | `curl /api/health` | 无 |
| 2 拆分结构 | 编译通过 | 无 |
| 3 配置管理 | 改 PORT 环境变量 | 无 |
| 4 数据库 | `/api/health` 返回 db/redis 状态 | PostgreSQL + Redis |
| 5 数据模型 | AutoMigrate 自动建表 | PostgreSQL |
| 6 Router 骨架 | 编译通过 | 无 |
| 7 中间件 | curl 看响应头 | 无 |
| 8 Repository | 单元测试 | 无 |
| 9 缓存+安全 | 编译通过 | 无 |
| 10 Auth | curl 注册/登录 | PostgreSQL |
| 11 HTTP 客户端 | 单元测试 mock | 无 |
| 12 核心业务 | curl 接口 | PostgreSQL |
| 13 AI+夜估+后台 | 编译通过 | 无 |
| 14 收尾 | 全量测试 | 全部 |
